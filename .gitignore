# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Build outputs
backend/dist/
frontend/dist/

# Environment variables (NEVER commit real .env files)
.env
backend/.env
frontend/.env

# Uploaded files & generated PDFs (user data — not in git)
backend/uploads/

# Local JSON database (user data — not in git)
backend/data/*.json

# macOS
.DS_Store

# Editor
.vscode/
*.swp
