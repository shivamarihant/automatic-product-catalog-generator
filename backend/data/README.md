This directory stores the local JSON database files.
These files are excluded from git (.gitignore) to protect user data.
The application creates products.json and catalogs.json automatically on first run.
